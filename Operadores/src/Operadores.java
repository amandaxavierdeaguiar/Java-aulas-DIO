public class Operadores {
    public static void main(String[] args) {
        int a, b;

        a = 7;
        b = 9;

        String resultado = a==b ? "verdadeiro" : "falso";
        /* Exemplo sem a forma reduzida, utilizando if/else
        String resultado = ""; (retirar esse caso utilize a opção reduzida)
        if (a == b)
            resultado = "verdadeiro";
        else
            resultado = "falso";
         */
        
        System.out.println (resultado);
    }
}
